name: Build and Push Docker Image to ECR

on:
  push:
    branches:
      - main

env:
  AWS_REGION: us-west-1
  ECR_REPOSITORY: demo/bluegreen

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Configure AWS credentials
      uses: aws-actions/configure-aws-credentials@v4
      with:
        aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
        aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
        aws-region: ${{ env.AWS_REGION }}

    - name: Login to Amazon ECR
      id: login-ecr
      uses: aws-actions/amazon-ecr-login@v2

    - name: Build and Push Image to ECR
      env:
        ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
        IMAGE_TAG: ${{ github.sha }} # Uses Git SHA as image tag
      run: |
        docker build -t $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG .
        docker push $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG

    # - name: Update Image in Kube Deployment
    #   id: setup-kube
    #   uses: azure/setup-kubectl@v4
    #   with: 
    #     version: latest
    
    # - name: Update Kube Deployment
    #   run: |
    #     echo "${KUBE_CONFIG_DATA}" | base64 -d > kubeconfig.yaml
    #     chmod 600 kubeconfig.yaml
    #     export KUBECONFIG=kubeconfig.yaml
    #     kubectl set image deployment/webapp-deployment webapp=$ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG --record
    #   env: 
    #     KUBE_CONFIG_DATA: ${{ secrets.KUBE_CONFIG_DATA }}