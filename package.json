{
    "dependencies": {
        "connect": "^3.7.0",
        "express": "^5.1.0"
    },
    "scripts": {
        "start": "node server.js",
        "dock:build": "docker build -t staticbluegreen:latest .",
        "dock:start": "docker run -v /var/run/docker.sock:/var/run/docker.sock -d -p 8080:8080 staticbluegreen"
    }
}
